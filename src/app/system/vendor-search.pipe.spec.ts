import { VendorSearchPipe } from './vendor-search.pipe';

describe('VendorSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new VendorSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
